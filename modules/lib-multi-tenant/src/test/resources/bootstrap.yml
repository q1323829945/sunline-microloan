spring:
  application:
    name: saas-multi-tenant

  profiles:
    active: default

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    generate-ddl: false
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    url: "jdbc:mysql://localhost:3306/db_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true"
    username: "root"
    password: "123456"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
